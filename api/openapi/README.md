# OpenAPI catalog

Per DD_API-00 §9, each module publishes `{module-code}.openapi.yaml` here before
frontend implementation starts (paths, schemas, auth, headers, errors,
pagination, idempotency, examples).
